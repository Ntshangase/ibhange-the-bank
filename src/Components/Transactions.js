import React from 'react'

// since all transactions are structured the same, this is a template for all transactions.
/* postOrder, cardNumber are the two arnormalies in this template, I think they are a mistake from 
the the person who created the data since cardNumber is an empty string and
 I don't know what postedOrder means in Banking. */
 
function Transactions() {
  return (
    <div>Transactions</div>
  )
}

export default Transactions